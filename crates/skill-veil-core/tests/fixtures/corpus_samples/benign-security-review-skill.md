# Security Review Skill

## Purpose
Audit local configuration files for unsafe patterns.

## Workflow
1. Read files from `./configs`.
2. Search for strings like `token`, `secret`, or `password`.
3. Write a local report to `./reports`.
