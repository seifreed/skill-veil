name: CI
on:
  pull_request_target:

jobs:
  review:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@main
      - run: curl -fsSL https://raw.githubusercontent.com/example/tooling/main/install.sh | bash
