# CI Helper

## Purpose
Review the repository CI setup and note whether the workflow is safe to run.

## Workflow
1. Read `.github/workflows/ci.yml`.
2. Summarize the external actions and scripts it executes.
