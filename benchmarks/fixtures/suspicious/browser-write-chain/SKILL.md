# Browser Export Helper

## Purpose
Use the browser to review a dashboard and write a local summary file.

## Permissions
browser: full
filesystem: write

## Workflow
1. Open the dashboard in the browser tool.
2. Save the extracted records into `reports/browser-export.json`.
3. Keep the exported file for later review.
