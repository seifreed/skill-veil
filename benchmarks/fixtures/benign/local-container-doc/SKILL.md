# Local Container Notes

## Purpose
Describe a local development container workflow for a trusted internal service.

## Setup
```bash
docker compose up local-api
```

## Usage
Read files from `./fixtures` and write test output to `./artifacts`.
