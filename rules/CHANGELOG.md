# Rules Changelog

## Unreleased

- added `rules/official/core.yaml` as the first external rule pack compatible with the runtime loader
- added fixture-pack support via `skill-veil rules test-pack`
- added taxonomy coverage for:
  - `persistent_prompt_tampering`
  - `tool_abuse`
  - `autonomy_escalation`
  - `social_manipulation`
